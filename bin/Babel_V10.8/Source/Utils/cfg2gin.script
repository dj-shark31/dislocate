
printUsage() {
	echo "Usage: $0 input_file output_file"
	echo "  input file with cfg format"
	echo "  output file with gin format"
	exit 1
}

if [ $# \< 2 ]; then
	printUsage
fi

cfgFile=$1
ginFile=$2

if [ ! -f $cfgFile ]; then
	echo "file $cfgFile does not exist"
	exit
fi

tmpFile=`mktemp`
echo > $tmpFile "
&input
  verbosity=0
  inpCfg=.true.
  inpFile='$cfgFile'
  outGin=.true.
  outFile='$ginFile'
&end
" 
$babel $tmpFile
rm -f $tmpFile
